# Required: OpenAI API Key
OPENAI_API_KEY=YOUR_API_KEY

# Required: Gmail OAuth Credentials
GMAIL_CREDENTIALS_PATH=path/to/credentials.json
GMAIL_TOKEN_PATH=path/to/token.json

# Optional: API Server Configuration
# API_HOST=0.0.0.0
# API_PORT=8000
# API_RELOAD=true

# Optional: CORS Origins (comma-separated)
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Optional: History Storage Configuration
# HISTORY_STORAGE_DIR=data/history
# HISTORY_MAX_FILES=100

# Optional: Environment
# ENVIRONMENT=development

# Optional: Image Processing Configuration
# IMAGE_PROCESSING_ENABLED=false
# IMAGE_MAX_SIZE_MB=10
# IMAGE_PROCESSING_TIMEOUT=60
# IMAGE_MAX_PER_EMAIL=5
# IMAGE_ALLOWED_DOMAINS=googleusercontent.com,gstatic.com
